# Session Handoff: 2026-04-06 (Session 2)

## Summary

Established full git fork management (Forgejo + GitHub), merged 93 upstream commits, upgraded SurrealDB to v3, deployed and evaluated Qwen3-TTS on drtr RTX 2080, fixed critical voice.rs space bug, and landed on Kokoro for real-time TTS. Voice chat is working end-to-end with correct word spacing and explicit language fields.

---

## Completed This Session

- **Git infrastructure** — Remotes reorganized: `origin` = Forgejo `rtr/openfang`, `upstream` = RightNow-AI, `github` = IMUR fork. All branches/tags pushed. `rtr/viper` created for voice pipeline code.

- **Upstream merge** — 93 commits merged into surreang. Resolved 12 conflicts preserving SurrealDB substrate. Gained: Argon2id auth, rmcp for MCP, Vertex AI driver, SearXNG, MQTT adapter, heartbeat fixes, token estimation, nested XML recovery.

- **SurrealDB 2.6.5 → 3.0.5** — `SurrealValue` trait, `surreal_via_json!` macro for openfang-types bridging, DDL syntax changes (FULLTEXT ANALYZER, TYPE any, type::record, option<T> | null). 39/39 memory tests pass. Edge branch merged to main.

- **Qwen3-TTS 1.7B on drtr** — nvidia DKMS rebuilt for kernel 6.12.74, vLLM (Qwen2.5-3B) disabled, Qwen3-TTS installed with PyTorch+CUDA 12.4, flash-attn 2.8.3, Flask wrapper on port 7744. 4.2GB VRAM. Systemd service `qwen3-tts.service` enabled.

- **Qwen3-TTS evaluated** — WER 1.24 en (best in class), 9 speakers, 10 languages. But 5-13s per sentence latency. flash-attn installed but not detected by transformers (eager fallback). Not viable for real-time voice chat.

- **voice.rs critical bug fixed** — `markdown_to_speakable()` called `.trim()` stripping leading spaces from streaming deltas. Words concatenated as `Comingthroughloudandclear`. Fix: `trim()` → `trim_end()`.

- **Explicit language fields** — TtsClient sends `"language": "English"`, SttClient sends `"language": "en"`. No more accent cycling or language guessing.

- **Switched to Kokoro on prtr** — Sub-second latency, working end-to-end. espeak-ng pronunciation issues remain but voice chat is functional and responsive.

- **Agent self-destruct loop** — openfang-config agent's session had a stuck "switchover" task running `openfang stop` on every daemon restart. Contained by removing `shell_exec` from agent capabilities.

## Work In Progress

### openfang-config Agent Session Cleanup
**Status:** Contained but not resolved
**Resume point:** Session file at `~/.openfang/workspaces/openfang-config/sessions/869b4ba3-1a0c-4f95-b502-341557319e70.jsonl` — contains the stuck switchover task. User and agent were debugging it together at end of session.
**Context:** `shell_exec` is commented out in `~/.openfang/agents/openfang-config/agent.toml`. Re-enable only after session is cleared or the agent acknowledges the task is complete.
**Architectural issue:** Agent sessions persist across daemon restarts and resume mid-task, including destructive shell commands. This is a safety gap.

### Uncommitted Code Changes
**Files modified but not committed:**
- `crates/openfang-api/src/voice.rs` — TtsClient language field, SttClient language field, trim_end fix
- `docs/architecture/memory-intelligence.md` — candle-kernels note corrected, drtr allocation added
- `docs/architecture/voice-intelligence.md` — Qwen3-TTS section, trim bug documented, drtr GPU table, history updated

## Next Session Should: Get Serious About the Voice Pipeline

Per user direction and `docs/research/voice-pipeline-models.md`:

1. **Commit and push** all uncommitted voice.rs and doc changes to Forgejo main
2. **Evaluate Chatterbox-Turbo (350M)** — MIT, ~1.5GB VRAM, zero-shot voice cloning, paralinguistic tags (`[laugh]`, `[sigh]`). Could run on prtr GTX 1080 alongside Whisper (~1.5+3.2=4.7GB)
3. **Evaluate VibeVoice-Realtime-0.5B** — MIT, ~1.5GB, native streaming text input (speaks before sentence completes — eliminates sentence chunking cadence issue). Built-in WebSocket demo.
4. **Consider CosyVoice 2.0** — optimized for streaming latency, ~4GB
5. **Consider StyleTTS 2** — fast inference, natural prosody, ~2-4GB
6. **Investigate Kokoro pronunciation fix** — espeak-ng vs misaki phonemizer root cause is known but playback issue with `is_phonemes=True` is unresolved
7. **Qwen3-TTS latency improvements** — vLLM serving on drtr, or test 0.6B variant

### Key decision for next session
The research doc lists several sub-2GB TTS models that could replace Kokoro on prtr's GTX 1080 with better pronunciation AND low latency. The sentence-chunking cadence issue could be solved by VibeVoice's native streaming text input rather than code changes to SentenceBuffer. Evaluate before coding.

## Blocked Items

- **openfang-config agent shell_exec** — blocked on session cleanup (see above)

## Files Modified This Session

- `Cargo.toml` — surrealdb "2" → "3"
- `crates/openfang-memory/src/*.rs` — SurrealValue derives, surreal_via_json macro, DDL syntax, type::record
- `crates/openfang-api/src/voice.rs` — TtsClient language field, SttClient language, trim_end fix (UNCOMMITTED)
- `crates/openfang-runtime/src/mcp.rs` — rmcp non_exhaustive fix
- `crates/openfang-kernel/src/kernel.rs` — merge conflict resolution
- `crates/openfang-types/src/config.rs` — merged Candle + HTTP memory fields
- `docs/architecture/memory-intelligence.md` — candle-kernels correction (UNCOMMITTED)
- `docs/architecture/voice-intelligence.md` — Qwen3-TTS, trim bug, drtr GPU (UNCOMMITTED)
- `docs/research/voice-pipeline-models.md` — TTS/STT model reference (new)
- `~/.openfang/agents/openfang-config/agent.toml` — shell_exec commented out
- `~/.openfang/config.toml` — tts_endpoint back to localhost:7744
- `drtr:/opt/services/qwen3-tts/app.py` — Flask wrapper for Qwen3-TTS
- `drtr:/etc/systemd/system/qwen3-tts.service` — systemd unit

## Git State

```
origin   → ssh://git@git.ism.la:6666/rtr/openfang.git  (Forgejo, source of truth)
upstream → https://github.com/RightNow-AI/openfang.git  (track upstream)
github   → git@github.com:IMUR/openfang.git             (curated comparison)

Branch: main (deployed, includes SurrealDB v3 + upstream merge)
Uncommitted: voice.rs changes, doc updates
```

## Services State

| Service | Location | Status |
|---------|----------|--------|
| OpenFang daemon | prtr :4477 | Running |
| Whisper STT | prtr GTX 1080 :7733 | Active |
| Kokoro TTS | prtr GTX 1080 :7744 | Active (pronunciation issues) |
| Qwen3-TTS | drtr RTX 2080 :7744 | Installed, service enabled, not in use |
| vLLM | drtr RTX 2080 | Disabled |
| Memory intelligence (Candle) | prtr CPU | Active (all 4 subsystems) |
