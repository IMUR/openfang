# Session Handoff: 2026-03-27 (Session 2)

## Read first (next agent)

1. **There are two handoffs from this date.** This file is **Session 2** (memory audit + P0 I/O fix). The other is [2026-03-27-handoff.md](2026-03-27-handoff.md) (async migration, usage API stubs). Read both if you are touching API routes or boot.
2. **Ground truth for SurrealDB tables** is the Rust code in `crates/openfang-memory/src/*.rs`, not this doc alone. Tables actually used today:
   - `sessions`, `canonical_sessions` — `crates/openfang-memory/src/session.rs`
   - `memories` — `crates/openfang-memory/src/semantic.rs`
   - `kv`, `agents` — `crates/openfang-memory/src/structured.rs`
   - `usage` — `crates/openfang-memory/src/usage.rs` (not `usage_events`)
   - `entities`, `relations` — `crates/openfang-memory/src/knowledge.rs`
   - `paired_devices`, `task_queue` — `crates/openfang-memory/src/substrate.rs`
3. **No schema DDL runs today.** `db::init()` only opens SurrealKV and sets namespace `openfang` / database `agents`. Everything is schemaless until you add `DEFINE` statements.

## Summary

SurrealDB memory-layer audit after the SQLite migration. Shipped one P0 fix: JSONL session mirror writes no longer block the Tokio runtime (`spawn_blocking`). Documented gaps (no DDL, brute-force vector recall, no-op consolidate, etc.). ML path (Burn vs Candle vs SurrealML) was **explicitly deferred** by the user — do not assume Burn is chosen.

## Completed this session

- [x] `cargo test -p openfang-memory` — **35 tests**, all passing (last verified this session).
- [x] **P0:** `write_jsonl_mirror` — async API, body runs on blocking pool via `tokio::task::spawn_blocking` + `write_jsonl_mirror_sync`.
  - `crates/openfang-memory/src/session.rs` — `Session: Clone`, `PathBuf` for mirror path
  - `crates/openfang-memory/src/substrate.rs` — `pub async fn write_jsonl_mirror(...)`
  - `crates/openfang-kernel/src/kernel.rs` — two call sites: clone session + `sessions_dir`, `.await`
- [x] Ops record: `/mnt/ops/records/prtr-records/2026-03-27-openfang-surrealdb-memory-audit.md`

## Not done (explicit)

- Schema DDL in `db::init()` — **not implemented**
- `sqlite_path` → `db_path` rename — **not done**
- README "SQLite + vector" row — **not updated**
- Native HNSW queries in `semantic.rs` — **not done** (still fetch + cosine in Rust)
- `Memory::consolidate()` — still returns empty report at `substrate.rs` ~595–604
- `max_depth` in graph queries — still ignored; see `knowledge.rs` ~116+
- New `openfang-embedding` crate — **not started**

## P0 next: schema DDL

**File:** `crates/openfang-memory/src/db.rs` — extend `init()` after `use_ns` / `use_db`.

**HNSW dimension:** Plan was to use **384** for MiniLM-class embeddings (matches `infer_dimensions` for `all-MiniLM-L6-v2` in `openfang-runtime`). If you standardize on a different model (e.g. Liquid ColBERT), **confirm output dimension from model docs** before `DEFINE INDEX` — wrong dimension breaks or silently mis-indexes vectors.

**Reference DDL patterns:** `docs/research/surrealdb-openfang-reference.md` section 4.

**Idempotency:** Existing user DBs already have rows. Prefer `IF NOT EXISTS` style defines where SurrealDB supports them, or tolerate errors on re-run; test against a copy of `~/.openfang/data/` if possible.

## P1 next (small, batch-friendly)

| Task | Where |
|------|--------|
| Rename config field | `sqlite_path` → `db_path` in `openfang-types` config + `kernel.rs` boot path |
| README feature table | `README.md` ~197 — Memory row still says "SQLite + vector" |

## Deferred (user choice)

- Embedding backend: Burn (`minilm-burn`) vs Candle (Liquid LFM family) vs SurrealML for in-DB inference — **no decision locked in**
- Weight packaging: bundle vs download-on-first-run
- See `docs/research/models.md` for Liquid model ↔ OpenFang mapping

## Plan files in repo

- `.cursor/plans/surrealdb_memory_layer_hardening_41e8f327.plan.md`
- `.cursor/plans/native_ml_integration_with_surrealdb_hardening_a78dd37c.plan.md`

## Commands

```bash
cd /home/prtr/prj/openfang   # or your clone

cargo test -p openfang-memory
cargo build --bin openfang   # headless: skip full workspace if desktop fails without GTK
cargo run --bin openfang start
```

## Session quality (meta)

Earlier in the same long chat, responses degraded (edit-loop failures, garbled text). If anything in this file conflicts with code, **trust the code**. Prefer a fresh agent context for DDL work.
